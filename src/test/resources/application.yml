spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:todotestapp
    driver-class-name: org.h2.Driver
    username: ambrizals
    password: pasasword
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  liquibase:
    enabled: false
